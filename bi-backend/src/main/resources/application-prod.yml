# 线上配置文件
server:
  port: 8101
spring:
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bi_backend
    username: root
    password: 自己的密码
  # Redis 配置
  redis:
    database: 1
    host: localhost
    port: 6379
    timeout: 5000
    password: 自己的密码
mybatis-plus:
  configuration:
    # 生产环境关闭日志
    log-impl: ''
